# RoomRecall 40-Iteration Ledger

This ledger keeps the prototype loop honest: an iteration counts only when it changes PRD/prototype/verification behavior, passes `node prototype/verify.mjs`, and produces a fresh `node prototype/self-loop.mjs` report.

## Completed

| Version | Iteration id | Countable change | Evidence | Next unlocked |
| --- | --- | --- | --- | --- |
| V01 | pre-loop-baseline | Spatial memory prototype with 3D cutaway, 2D plan, scan proposals, product intake, coordinate anchors, and layout metrics. | Browser verification existed with 47 assertions passing before the stricter self-loop. | Split dense layout UI and make correction behavior verifiable. |
| V02 | rr-5e3c5580fb19 | Right panel split into Selection, Retrieval Plan, Spatial Frame, Layout Health, Scan Diff, and Contract; 3D furniture made state-driven; 2D semantic labels fixed; affected placement names surfaced. | 54 verification assertions passing; self-loop 10/12. | Commit corrected placements and shape backend mutation bundle. |
| V03 | rr-5a40824e9b8e | Not-there correction became a committed placement ledger entry; future locate reads the committed corrected placement; backend exposes mutationBundle. | 57 verification assertions passing; self-loop 12/12. | Promote 3D support surfaces from visual sub-volumes to placement targets. |
| V04 | rr-fe7304afdf80 | Furniture/container support surfaces are derived from Place Graph state, selectable in 3D, usable as placement targets, and exposed as backend supportSurfaceRecords. | 62 verification assertions passing; self-loop 12/12. | Add support-surface collision and fit prechecks before confirming placement. |
| V05 | rr-616e0c710089 | Support-surface placement is now precheck-first: allowed checks do not mutate placement, confirmation carries precheck/surface references, occupied surfaces block with conflicting placement ids, oversized items fail fit/opening checks, and parent furniture edits stale prior prechecks. | 76 verification assertions passing; self-loop 13/13. | Add hover affordance and surface-ownership tests for targetable 3D support surfaces. |
| V06 | rr-b583cdd47a2c | 3D support surfaces now have a non-mutating hover affordance: hover exposes target id, owner, candidate item, ready/blocked preview, DOM chip, and stronger mesh state before selection; supportSurfaceRecords distinguish usable footprint, clearance height, render thickness, ownerRef, and placeRef; support precheck copy maps allowed to ready and stale takes priority over confirmed. | 87 verification assertions passing; self-loop 14/14. | Add causal ids for support collision checks and make ready/blocked/stale copy survive backend commits. |
| V07 | rr-441e1109a419 | Support-surface checks now round-trip causal ids and copy snapshots: check records reference precheck ids, placement op ids, geometry op ids, UI state snapshots, and support-local occupied patches; rendered backend preview preserves ready, confirmed, blocked, confirmed-to-stale, and blocked-to-stale copy; mutation bundle exposes manual geometry ops that cause stale support checks. | 113 verification assertions passing; self-loop 15/15. | Add support-pose snapping so occupied patches can move away from center instead of treating every surface as one slot. |
| V08 | rr-5fe8ffe48dd2 | Support surfaces now support local patch snapping: when the desk center is occupied by earphones, the charger precheck auto-snaps to a right-side free patch, stores the snapped candidate pose in backend placement ops, renders shared-surface occupancy patches in 3D, and only blocks a saturated desk-wide tray when no free patch remains. | 120 verification assertions passing; self-loop 16/16. | Promote support prechecks into direct drag previews so the user sees fit, collision, and stale status before dropping. |
| V09 | rr-557591696d22 | Direct drag preview is now a first-class transient contract: 2D/3D previews show ready, snapped, blocked, and stale support outcomes before drop; preview volume/patch ghosts render without mutating placement; backend exposes dragPlacementPreviews, dragPlacementGate, activeDragPreview, and preview-causal support checks separate from persisted prechecks. | 132 verification assertions passing; self-loop 17/17. | Add editable patch handles and object-volume resize controls so users can choose the local placement instead of relying on auto-nearest packing. |
| V10 | rr-54410e65afd6 | Manual support-local patch preview is now modeled: scripted/preview users can choose a desk-local coordinate such as x=-45cm, get a clear manual preview or an occupied manual-patch block at center, see the same state in 2D plan preview and backend drag preview records, and keep placement unmutated before drop. | 138 verification assertions passing; self-loop 18/18. | Expose manual patch movement as visible 2D/3D handles and add object-volume resize controls. |
| V11 | rr-a4ad1f18c897 | 2D plan previews now expose separate manual patch, object-volume, and handle layers instead of one collapsed preview box; the verifier proves the plan patch and volume dimensions match the 3D drag ghost geometry. | 140 verification assertions passing; self-loop 19/19. | Persist the chosen manual patch through commit records, then make the 2D handle pointer-draggable. |
| V12 | rr-972aa2a1069b | Manual patch commit is now end-to-end auditable: the chosen support-local coordinate survives from preview into persisted precheck, support placement op, placement record, support-surface occupancy, latestPrecheck, and the rendered backend contract; drag previews now link back to the persisted precheck id after commit. | 147 verification assertions passing; self-loop 20/20. | Make the 2D patch handle pointer-draggable while reusing the same commit-readback contract. |
| V13 | rr-f803c3e1148e | Support-surface placement now has an append-only commit boundary: confirmed manual/support placements append support placement commit ledger entries, placement records carry support commit ids, final post-commit readback clears active drag previews, and manual support-local coordinates survive later surface prechecks plus parent geometry invalidation. | 152 verification assertions passing; self-loop 20/20. | Make the 2D patch handle pointer-draggable as a preview-only gesture. |
| V14 | rr-9f366c11013d | The 2D plan patch handle is now a real pointer-draggable preview control: dragging the center puck changes the support-local coordinate, updates the 2D plan, 3D ghost, and backend activeDragPreview, and leaves placement plus persisted prechecks unchanged until explicit commit. | 155 verification assertions passing; self-loop 21/21. | Add preview-only volume resize handles and candidate dimensions. |
| V15 | rr-00b217c00e97 | The 2D plan volume outline now has a preview-only resize handle: dragging the southeast handle changes candidate width/depth, mirrors into the 3D volume ghost and backend activeDragPreview fit/support patch, and keeps placement, precheck state, and the item's stored footprint unchanged before commit. | 158 verification assertions passing; self-loop 22/22. | Add explicit confirmation for resized dimensions as separate geometry plus placement commit records. |
| V16 | rr-5bba08903e93 | Resized dimensions now have an explicit confirmation boundary: released resize previews become `Size Draft` UI with `Confirm Size + Placement` / `Discard Resize`, confirmation updates the item footprint and 3D footprint shadow, appends separate `commit_item_geometry_update` and `commit_support_surface_placement` ops, links placement records and support occupancy to the geometry op id, and keeps geometry ops out of placement buckets while exposing them in commitPreview. | 162 verification assertions passing; self-loop 23/23. | Move the same producer-review-commit boundary into scan identity observations and match/create/merge proposals. |
| V17 | rr-f7763a441427 | Scan identity now has a producer-only observation seam: `scan-identity-producer-v1` emits typed item/container observations for item candidate, contents seen, seen-empty, occluded/unknown, and privacy-redacted regions; observations carry priors, visibility, privacy, and resolution requirements but do not bind canonical records or invent room-level coordinates; product-created items can participate as priors without becoming truth writes. | 169 verification assertions passing; self-loop 24/24. | Promote identity observations into match/create/merge and coverage proposal contracts without adding commit actions yet. |
| V18 | rr-b5e9fab6206f | Identity observations now derive read-only proposal contracts: item match, item merge, seen-empty coverage, contents-unknown coverage, and privacy hold records carry observation ids, candidate refs, allowed actions, required resolution, and commit effects while remaining non-commit-ready; Scan Diff renders identity proposal rows with Review only and no Accept/Reject/Commit controls. | 172 verification assertions passing; self-loop 25/25. | Turn read-only identity proposal rows into a shared review surface with selected observation focus and match/create/merge decisions. |
| V19 | rr-8deda225b4d7 | Identity proposal rows now form a shared review surface: Review focuses the selected observation/proposal, allowed actions such as `match_existing` create local `identityResolutionDrafts`, active rows show draft status, backend readback exposes the draft, and geometry proposals plus identity commit ops remain unchanged. | 174 verification assertions passing; self-loop 26/26. | Promote reviewed identity resolution drafts into an atomic scan identity commit ledger with undo/source lineage. |
| V20 | rr-b96388316904 | Reviewed identity drafts now commit into append-only scan identity ledger entries: `commit_identity_resolution` / `commit_coverage_resolution` ops carry proposal, observation, producer, action, candidate, visibility, privacy, and required-resolution fields while staying out of geometry and placement write buckets. | 175 verification assertions passing; self-loop 27/27. | Use committed identity resolution as locate answer evidence, then expose source lineage and append-only rollback. |
| V21 | rr-356cbc20cbe7 | Committed identity resolutions now feed a read-only locate answer model: explicit `targetItemId` prevents candidate-prior spread, `locateAnswer("lamp-shaped object")` returns the matched product item with scan identity evidence while placement stays sourced from the existing placement record, Scan Diff/detail expose lineage, and rollback appends a `scan_identity_reversal` commit without deleting history or leaking into geometry/placement writes. | 178 verification assertions passing; self-loop 28/28. | Use answer lineage as the retrieval-agent explanation surface, including confidence and stale/reverted identity handling. |
| V22 | rr-4a130c989f3e | Retrieval answers now use a stable answer envelope for `answered`, `needs_review`, and `not_found`: the latest matching identity lineage wins even when reverted, older active identity commits are blocked instead of silently reused, backend `answerPreview` stays non-null for no-result queries, and the right panel exposes confidence breakdown, suppressed evidence rows, review actions, and no-result hooks. | 180 verification assertions passing; self-loop 29/29. | Carry retrieval explanation into 2D/3D overlays so answer confidence, holds, and suppressed evidence are visible in spatial context. |
| V23 | rr-713d0c94dc2c | Retrieval answer states now project into space: 2D plan overlays show answered / needs-review / not-found states with answer source, confidence, placement, identity commit, suppressed commits, and hold anchors; 3D labels and item halo metadata show item answers, identity holds, and scene-level no-result overlays; backend snapshots expose the same `planAnswerOverlay` / `retrievalSpatialOverlay` record. | 183 verification assertions passing; self-loop 30/30. | Use spatial retrieval overlays to reduce dense 2D labels and reveal answer evidence on hover/selection. |
| V24 | rr-a0f50b9a9318 | The 2D plan now uses an explicit label-density policy: secondary pin labels collapse in dense views, answer targets and needs-review hold anchors remain visible, no-result keeps every item label collapsed, hover/focus can reveal a secondary label, and the whole label reveal path is read-only with ARIA/focus hooks and no geometry, placement, or identity writes. | 188 verification assertions passing; self-loop 31/31. | Promote 3D object refinement with dimension-scaled archetypes and clearer object silhouettes. |
| V25 | rr-185add6001d0 | 3D item meshes now resolve dimension-scaled archetypes instead of generic cubes: bottles, shoes, folded cloth, rolled towels, loops, cards, books, bags, chargers, and desk lamps scale their real silhouette to the item footprint; lamp/charger product-intake objects render specialized parts, and resize commits update the actual 3D silhouette bounds, not only the footprint shadow. | 192 verification assertions passing; self-loop 32/32. | Add selected-object 3D volume outlines and container occupancy silhouettes for dense storage surfaces. |
| V26 | rr-c635cca0657b | Selected/retrieval objects now expose true 3D volume outlines: answer targets render teal footprint-derived outlines, identity holds render gold outlines without answer promotion, and not-found clears item outlines; support-surface occupancy patches are promoted into `containerOccupancySilhouettes` with item archetype, occupied patch, world pose, source placement, and backend readback. | 197 verification assertions passing; self-loop 33/33. | Add selected-object local axes, rotation handles, and container fit readouts for 3D manipulation. |
| V27 | rr-e5db63564ff6 | Selected 3D objects now expose manipulation readouts: local x/y/z axes, a rotation handle, footprint-derived volume outline, and container/support fit readout in both the detail panel and backend preview; these visual affordances are read-only and do not append geometry, placement, or identity ops. | 200 verification assertions passing; self-loop 34/34. | Add layout-planning scenario compare for current vs proposed furniture placement and storage gain/loss. |
| V28 | rr-94554de6a5f1 | Layout planning scenarios are now first-class read-only compare records: desk wall alignment, vertical shelf storage, and entry rack flush each expose current/proposed metrics, clearance/storage/collision deltas, ranking, DOM rows, backend `layoutScenarioCompareRecords`, and 2D plan scenario ghosts while preserving canonical furniture and write ops. | 204 verification assertions passing; self-loop 35/35. | Promote layout scenarios into explicit geometry-diff ids and predicted placement impact records. |
| V29 | rr-9934002ce237 | Layout scenario records now carry stable `geomdiff-*` ids with before/after geometry deltas and scenario-only predicted placement impact records. The panel, 2D ghosts, layout snapshot, and backend preview all preserve geometry diff ids while proving canonical furniture, placements, commit ledger, geometry ops, and placement ops are unchanged. | 206 verification assertions passing; self-loop 36/36. | Carry scenario impact into support-surface impacts and recommendation reason codes. |
| V30 | rr-38637a3c9eff | Scenario impact now reaches support surfaces and explanation: each geometry diff forecasts support-surface pose shifts/precheck refresh needs, and each scenario exposes machine-readable recommendation reason codes with benefit/cost sentiment and evidence refs that match the panel and backend preview. | 208 verification assertions passing; self-loop 37/37. | Connect scenario recommendation reasons to scan quality, certainty, and guided capture prompts. |
| V31 | rr-7428a62f3a89 | Layout recommendation certainty now incorporates scan quality: each scenario carries coverage/occlusion/scale/drift/anchor checks, `needs_capture_before_commit` status, guided capture prompts tied to geometry diff ids, and `scan_quality_capture_needed` recommendation reasons so weak visual evidence becomes explicit work instead of hidden confidence. | 210 verification assertions passing; self-loop 38/38. | Turn scan certainty into editable anchors, keyframe coverage, and reconstruction job contracts. |
| V32 | rr-c8eb3f0b390c | The scan layer now exposes keyframe coverage and a proposal-only reconstruction job contract: selected frames, camera path, room envelope, furniture cuboids, missing coverage groups, private/unknown regions, and blocked reason codes round-trip through scan pipeline, backend contract, layout snapshot, and the scan panel without writing canonical layout. | 214 verification assertions passing; self-loop 39/39. | Make anchors editable and turn anchor changes into stale scan-derived geometry. |
| V33 | rr-2ac664b428c1 | Anchor edits are now draftable: `previewAnchorEdit` creates a non-committing anchor draft, shows it in the scan panel, round-trips it through backend/layout snapshots, and marks scan-derived furniture geometry stale in draft scope while proving canonical anchors, furniture, commit ledger, and geometry ops are unchanged. | 218 verification assertions passing; self-loop 40/40. | Promote anchor edits into explicit commit/reject boundaries with reconstruction refresh proposals. |
| V34 | rr-22d880ea5826 | Anchor edits now have explicit resolution boundaries: committing a draft updates the anchor through an append-only `anchor_edit` ledger entry, records scan-derived stale geometry ops, and creates a proposal-only reconstruction refresh record; rejecting a draft records history without changing anchors or refresh proposals. | 221 verification assertions passing; self-loop 41/41. | Carry committed anchor changes into scenario certainty and locate/layout readbacks. |
| V35 | rr-6691977c0444 | Committed anchor edits now propagate into downstream readbacks: backend anchor records expose the anchor commit/status, layout scenario certainty references the latest anchor commit, and locate answers carry coordinate frame, scan session, and latest anchor commit context. | 224 verification assertions passing; self-loop 42/42. | Add scenario commit gate that merges anchor, reconstruction, support, identity, and placement blockers. |
| V36 | rr-d37f0853fae3 | Layout scenarios now expose a unified commit gate: placement, support-surface, scan-quality, reconstruction, identity/coverage, and privacy blockers are merged into machine-readable gate records with evidence refs and required actions that round-trip through panel, layout snapshot, and backend preview. | 225 verification assertions passing; self-loop 43/43. | Add scenario apply/reject drafts that can consume the commit gate when blockers are cleared. |
| V37 | rr-2045b94fe294 | Layout scenario decisions now have explicit apply/reject boundaries: apply requests consume the commit gate and produce a blocked non-writing decision draft, while reject records decision history without changing furniture, commit ledger, or geometry ops; both surfaces round-trip through panel, backend, and layout snapshot. | 229 verification assertions passing; self-loop 44/44. | Add scenario focus/readback parity across panel, 2D plan, backend, and locate context. |
| V38 | rr-c85656474d36 | Scenario focus now has cross-surface parity: focusing `scenario-shelf-vertical-storage` drives the active layout row, focus summary, 2D scenario ghosts, backend/layout focus records, and locate answer context through the same scenario/focus ids. | 233 verification assertions passing; self-loop 45/45. | Freeze deterministic scenario fixtures and add replay verification. |
| V39 | rr-4c94745c8080 | Layout scenario recommendations now have deterministic replay fixtures: each scenario freezes decision-critical geometry diffs, metric deltas, commit-gate blockers, certainty prompts, reason codes, and predicted impacts; the focused scenario can replay by scenario id or fixture id through backend/layout snapshots without mutating furniture, placements, ledger, or write ops. | 238 verification assertions passing; self-loop 46/46. | Add an end-to-end demo trace from scan capture through reconstruction, scenario gate, human decision, anchor commit, and locate readback. |
| V40 | rr-8d44dc547269 | The prototype now exposes one auditable end-to-end RoomRecall chain: phone-scan keyframes feed a proposal-only reconstruction job, scenario compare/replay and commit gate stay read-only, accepted scan geometry writes through a `scan_review` ledger entry, anchor/identity/support/placement commits are named as Place Graph writes, and locate readback carries the committed anchor plus preview scenario context while private/unknown regions remain guarded. | 247 verification assertions passing; self-loop 47/47. | Promote the strongest prototype paths into a product PRD and decide which V1 surface should become real. |

## Remaining Iteration Bands

| Range | Theme | Target outcome |
| --- | --- | --- |
| V17 | Scan identity producer | Emit typed item/container identity observations from the scan pipeline before any truth write. |
| V18 | Scan identity proposal contract | Add match/create/merge and unknown/empty/private coverage proposals with required resolution fields. |
| V19 | Scan identity review surface | Reuse Scan Diff, 2D overlays, and shared selection for match/create/merge/reject decisions. |
| V20 | Atomic scan identity commit | Fold identity, merge, coverage, geometry, and placement ops into one scan commit ledger entry. |
| V21-V24 | 2D density | Label collapse, hover reveal, mobile plan review, and visual overlap checks. |
| V25-V28 | 3D object refinement | Dimension-scaled archetypes, support surface snapping, container occupancy silhouettes, and selected-object volume outlines. |
| V29-V32 | Layout planning | Furniture fit suggestions, route clearance alternatives, storage-volume optimization, and scenario compare. |
| V33-V36 | Agent retrieval | Kit readiness, negative evidence ranking, non-room states, and answer confidence explanations. |
| V37-V40 | Scan pipeline UX and hardening | Capture checklist, local/privacy modes, quality gates, proposal confidence explanations, accessibility/mobile polish, persisted JSON fixture, and end-to-end demo script. |

## Current Command

```bash
cd /Users/renjunbin/Documents/codebase/try/trello/room-recall/prototype
node verify.mjs
node self-loop.mjs
```
